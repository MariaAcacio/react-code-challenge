export enum CollectionNamesEnum {
  POKEMONS = "pokemons",
  USERS = "users",
}

export enum AttributeNamesEnum {
  ABILITIES = "abilities",
  TYPES = "types",
}
export enum AttributeSingularNames {
  ABILITY = "ability",
  TYPE = "type",
}
